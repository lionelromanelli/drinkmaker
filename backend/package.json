{
  "name": "drinkmaker-backend",
  "version": "1.0.0",
  "description": "Backend service for drinkmaker app with Hono + Bun",
  "main": "server.js",
  "scripts": {
    "start": "bun run server.js",
    "dev": "bun run --watch server.js",
    "lint": "eslint . --ext .js,.ts",
    "lint:fix": "eslint . --ext .js,.ts --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "pre-commit": "^1.2.2"
  }
}